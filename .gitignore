# 보너스 자료 (저작권)
(보너스프로그램) 오토워커.zip
오토워커/

# Node
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo

# 환경변수
.env
.env.local
.env.*.local

# Claude Code 로컬 설정 (스킬 정의는 커밋, 개인 설정만 제외)
.claude/settings.local.json

# 로그
*.log
npm-debug.log*
yarn-debug.log*

# 시스템
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# 런타임 산출물 (root의 runs/만 무시 — app/runs, lib/runs는 코드)
/runs/
/channels/*/output/

# SQLite 로컬 DB
local.db
local.db-shm
local.db-wal
*.db
*.db-journal
*.db-shm
*.db-wal
